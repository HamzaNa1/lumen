# Independent server identity

Every installation persists a random server identity. A desktop connection is keyed by an approved origin plus user identity, not by a globally trusted server UUID. The UUID detects identity changes but is not proof of identity.
